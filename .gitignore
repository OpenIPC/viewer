## .NET / Visual Studio
bin/
obj/
*.user
*.suo
.vs/
.idea/
.vscode/
*.swp

## Build output
[Bb]uild/
[Pp]ublish/
*.binlog

## Test results
TestResults/
*.trx
coverage*.xml
coverage*.json
*.coverage

## NuGet
*.nupkg
*.snupkg
.nuget/

## Local download cache (FFmpeg archives, sample assets)
.cache/

## OS
Thumbs.db
.DS_Store

## Claude Code harness local state
.claude/

## Local config
appsettings.Local.json
appsettings.Development.json

## OpenIPC.Viewer specific
# native runtime libs not checked in (per Phase 2 plan)
runtimes/win-x64/native/
runtimes/linux-x64/native/
runtimes/osx-x64/native/
runtimes/osx-arm64/native/
runtimes/android-arm64/native/
runtimes/android-x64/native/
# FFmpeg cross-compile scratch tree (build-ffmpeg-android.sh output)
build/ffmpeg-android-*/

# Web React SPA (Phase 21): node deps + the Vite build output that lands in the
# Web project's wwwroot. The MSBuild target rebuilds wwwroot on publish; the
# lockfile (package-lock.json) IS tracked.
node_modules/
src/OpenIPC.Viewer.Web/wwwroot/

## Local-only docs (planning, architecture, design-system, CLAUDE notes).
## Kept on disk for reference but not tracked.
/*.md
!README.md
